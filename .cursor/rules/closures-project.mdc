---
globs: projects/closures/**/*.rs
---

# 闭包项目学习指南

## 项目目标

学习 Rust 中的闭包概念，理解函数式编程在 Rust 中的应用。

## 核心概念

### 闭包语法对比

```rust
// Rust 闭包
|| self.most_stocked()           // 无参数
|x| x + 1                        // 单参数
|x, y| x + y                     // 多参数

// JavaScript 箭头函数
() => this.mostStocked()         // 无参数
x => x + 1                       // 单参数
(x, y) => x + y                  // 多参数
```

### 闭包特性

1. **环境捕获**：闭包可以访问外部作用域的变量（如 `self`）
2. **惰性求值**：`unwrap_or_else()` 只在需要时才执行闭包
3. **类型推断**：编译器自动推断闭包的参数和返回类型

## 关键方法

- `unwrap_or_else(closure)` - 类似 JS 的 `|| defaultValue()`
- `map(closure)` - 类似 JS 的 `array.map()`
- `filter(closure)` - 类似 JS 的 `array.filter()`

## 学习重点

1. 理解闭包与普通函数的区别
2. 掌握闭包的语法和使用场景
3. 学会在函数式编程中使用闭包
4. 理解闭包如何捕获环境变量

## 实践建议

- 尝试修改闭包逻辑，观察行为变化
- 实验不同的闭包语法形式
- 对比 Rust 闭包与 JS 箭头函数的异同
