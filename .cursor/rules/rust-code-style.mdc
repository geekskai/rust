---
globs: *.rs
---

# Rust 代码风格和最佳实践

## 代码注释风格

为初学者添加注释时：

1. **对比注释**：在复杂概念旁边添加 JS 等价写法
2. **概念解释**：解释 Rust 特有的概念（所有权、借用等）
3. **实用提示**：说明为什么这样写，避免什么问题

## 常见模式解释

- `#[derive(Debug, Clone)]` - 自动实现调试打印和克隆功能
- `.unwrap_or_else()` - 提供默认值的函数式方法
- `.clone()` - 创建副本，解决所有权问题
- `&` - 借用引用，不转移所有权
- `mut` - 声明可变变量

## 错误处理建议

当遇到编译错误时：

1. **所有权错误**：考虑使用 `.clone()` 或借用 `&`
2. **类型错误**：检查是否需要添加 trait（如 Debug, Clone）
3. **生命周期错误**：简化代码结构或使用 `clone()`

## 学习进度跟踪

建议按以下顺序学习项目：

1. `hello_world/` - 基础语法
2. `variables/` - 变量和可变性
3. `functions/` - 函数定义
4. `closures/` - 闭包和函数式编程
5. `generics/` - 泛型编程
